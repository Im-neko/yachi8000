node_modules/
dist/
.env
*.local

# ランタイム状態（SQLite・音声バッファ・合成キャッシュ）
data/

# 3D モデル。**再配布が許されたサンプルでも置かない**（容量。→ F-62, D-36 の 3）。
# 置き場所は設定ファイルと同じ側（開発は agent/data/、本番は PVC）。
*.vrm

# コード解析ツールのローカルキャッシュ
.serena/

# エージェントの作業領域（worktree を含む）。**中に git リポジトリが入る**ので、
# `git add -A` で巻き込むと embedded repository として取り込まれてしまう。
.claude/
